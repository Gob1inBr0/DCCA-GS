# Core runtime -- torch/torchvision should be installed first to match the
# CUDA version on the target machine (e.g. conda/pip with the right index).
torch>=2.1
torchvision>=0.16

numpy>=1.24
scipy>=1.10
Pillow
imageio
tqdm
tyro>=0.8
pycolmap>=3.10
plyfile
torchmetrics>=1.0
tensorboard

# gsplat -- use the local checkout for development:
#   pip install -e /Users/chen/Documents/gsplat-main
# or install the PyPI release:
#   pip install gsplat

# HAC++ module (scaffold_gs/hacpp.py + hacplus/) additionally requires the
# CUDA extensions from the official HAC-plus repo:
#   _gridencoder, arithmetic, simple_knn, torch_scatter
# On the 5090 server these are pre-installed in the `HAC_5090_a100` conda env.
