| 09-26 | 10962bb/0216a12（b3-progressive-quant 分支，已推 origin） | 实验（B3 启动） | **B3 渐进感知量化训练：实现 + 审查 + 双臂发射**。基础选 d34af30（服务器归档工作目录的最后提交，ph1v3/jr2 系列实际训练代码线；本地 codex/importance-experiment@d547ea8 因 model.py/hacpp.py 字段不一致无法运行训练，弃用）。实现：config 三字段 `coarse_ladder_align/start_iter/weight`（默认关/24000/0.05）；`generate_gaussians` 训练分支内在自适应 Q（含 content-aware 调整）定型后、加噪声前，计算连续符号到阶梯倍数的偏移惩罚 |r−L·round(r/L)|/L（feat/offset L=8、scaling L=2，与码流基层步距一致）；Q 与 mask 均 detach（子代理审查修掉 mask 梯度泄漏 + ramp=0 窗口白建图两处）；权重随剩余步数线性升满；trainer 每 500 步打印 raw/加权值。部署：服务器新建工作目录 /mnt/newproject2/work_dirs/b3（clone 归档 + GitHub fetch 分支），冒烟 10,600 步（ladder 10,050 起效）在 GPU2 验证中；双臂 30k 已发射（GPU3 `b3ab_base_r085_lam0005_s42` 对照、GPU4 `b3ab_b3_r085_lam0005_s42` 开 B3），协议与 ph0c base 完全一致（spa r085 + mini-splat + seed42 + λ0.0005 + 1-78），数据用 /mnt/newproject2/data/1-78_train。**预登记门不变：P0 前缀 +≥0.3 dB（150 视角）且满精度档不降**；跑完后对两臂 run 目录跑 c25 渐进码流管线对比。同代码双臂设计消除跨代码混淆 |
| 09-26 | （本次提交） | 运维（目录整理二） | **/mnt/newproject2 根目录整理**（用户指令"把 /mnt 下面的东西整理一下"）：`run_shell/` 的 201 个 c25 调试日志（30M）归档至 `archive/run_shell_logs_20260923/`；根目录空文件 `dl_data.log`、空目录 `results/` 删除；`sharc_validate.sh` 归档去重；`tmc13` 空壳目录与 `tmc13_full_20260923` 合并为单一 `tmc13/`，`/usr/local/bin/tmc3` 链接同步改指 `/mnt/newproject2/tmc13/build/tmc3/tmc3`（已验证 tmc3 v23.0 可用）。根目录现为 9 项：DCCA-GS、SHARC-GS、dcca_runs、runs、data、archive、work_dirs、gsplat-main、miniconda3（安装目录不可移）。迁移清单补记 2 已写入。当天 SSH 连接频繁被服务器重置（约每 1–2 分钟放行一次），改用单命令分步执行完成 |
| 09-26 | （本次提交） | 实验（冒烟判定，通过） | **B3 冒烟通过**：10,600 步短跑（阶梯惩罚 10,050 步提前生效，权重 0.05），`[B3] iter 10500: ladder raw 0.4941 weighted 0.02021`——数值与设计公式严格一致（0.05 × ramp 450/550 × 0.4941 = 0.0202），加权惩罚约为重建损失的 20%，量级合适；全程无崩溃、损失正常（0.06–0.13），梯度回传正常。冒烟产物（276M run 目录 + 脚本）已清理。双臂 30k（GPU3 对照 / GPU4 B3）健康运行中，B3 臂到 24,000 步惩罚生效点预计还需约 4–5 小时 |
