# DCCA-GS 论文后续实验计划

> 更新日期：2026-08-28
> 目的：明确初稿完成后还需补齐的实验、资源安排、判定标准和回写责任。

## 1. 当前已完成

| 方向 | 状态 | 结论 |
| --- | --- | --- |
| I2/I6 解码端可重算量化 + 渲染敏感度监督 | 已完成 | playroom 110k：I6-off 掉 0.064 dB；SPA 低码率下掉 0.42 dB |
| MLP 混合精度量化 | 已完成 | 推荐 cd8/rest16；4-28 110k 5.6471→5.4852 MiB，PSNR 基本不变 |
| SPA 训练侧 ADMM 剪枝 | 已完成 | 同锚点数比 topk 高 5.52 dB；低码率 0.96 MiB |
| MiniSplat depth-reinit | 已完成 | playroom +0.199 dB；drjohnson/T&T truck/4-28 为负，属场景依赖 |
| MiniSplat 完整版（贡献面积版） | 进行中 | `codex/minisplat-full` 已启动 7 场景泛化矩阵，未出最终结果 |

## 2. 第一优先级：完整 MiniSplat 泛化矩阵

### 2.1 目标

回答：把 depth-reinit 升级为 blur-split + contribution-area + intersection-preserving simplification 后，能否把当前“只 playroom 有效”变成跨场景正增益。

### 2.2 运行（LYH）

已有任务：

```
lyh_full_playroom       GPU4
lyh_full_drjohnson      GPU0 等待/训练
lyh_full_tandt_train    GPU1 等待/训练
lyh_full_tandt_truck    GPU2 等待/训练
lyh_full_mip_garden     GPU3 等待/训练
lyh_full_mip_bicycle    GPU5 等待/训练
lyh_full_mip_stump      GPU6 等待/训练
```

完成标记：

```
/home/T0ng/runs/lyh_full_matrix_done
```

### 2.3 收口动作

1. 对每个 `lyh_full_*` 读 `decoded_eval/metrics.jsonl` 和 `bitstreams/hac_meta.json`；
2. 与同场景旧 cell1/cell2 对比 PSNR、SSIM、LPIPS、total_MB、编码锚点数；
3. 对每个场景计算完整版相对 depth-reinit 版的 ΔPSNR 与 ΔMB；
4. 若 7 场景中 ≥4 正且替代大场景不显著为负，则写“完整版泛化有效”；
5. 否则写“完整版仍场景依赖”，关闭推广。

## 3. 第二优先级：必补实验（论文完整性）

### 3.1 大场景 110k 完整版（数据集待定）

- **协议**：大型场景 110k，SPA 0.85，MiniSplat full ±；
- **数据**：暂不使用 4-28、3-07、1-100；后续从 `/mnt` 数据包中选择替代数据集；
- **目标**：验证大场景是否仍负增益；确认是否与 depth-reinit 一致；
- **时间**：约 6–10 小时/组，建议 SHARC 任务结束后跑；
- **状态**：暂停，等用户确定 /mnt 数据包。

### 3.2 多 seed

- seed 42 + seed 2026，playroom、drjohnson、T&T train/truck、Mip garden/
  bicycle/stump；
- 目标：证明 ≥4/7 正、跨 seed 的标准差小于平均增益的一半；
- 已具备旧 seed 2026 drjohnson cell2 结果，需补完整版 seed 2026。

### 3.3 解耦消融

- `MiniSplat off / SPA off`
- `MiniSplat on / SPA off`
- `MiniSplat off / SPA on`
- `MiniSplat on / SPA on`

判定：若“Mini on/SPA off - baseline”与“Mini on/SPA on - SPA baseline”相近，说明增益来自位置增密而非 SPA 交互。

### 3.4 缺失的 I2/I6 单点

- 大场景单独 I6-off（待新数据集；当前只有 DB 110k I6-off）；
- SPA ratio 0.3/0.2；
- 110k DB 泛化（playroom/drjohnson 已具备部分，补完整 RD 曲线）。

## 4. 第三优先级：论文展示与严谨性

1. 绘制正式 RD 图：
   - 现有 `docs/figures/db_rd_curve.png`
   - 现有 `docs/figures/e4_428_rd.pdf`
   - 补 5 组件曲线（HAC++、I2+I6、+MLP quant、+SPA、+MiniSplat）；
2. 做 `codec_roundtrip_diagnostics`：bit-exact mismatch=0；
3. 统计训练时间、编码/解码时间、峰值显存；
4. 补结构分析：valid anchors、valid offsets、offsets per anchor；
5. 补负面结果表：I1、P0、R、I5、I6 side-info、语义；
6. 补 LPIPS 权重校验说明：lyh 已放 `/root/.cache/torch/hub/checkpoints/vgg16-397923af.pth`。

## 5. 判定门

| 门 | 条件 | 通过动作 |
| --- | --- | --- |
| G_full | 完整版 7 场景 ≥4 正且替代大场景不显著负 | 合入 `codex/minisplat-full` |
| G_multiseed | ≥3/5 场景跨 seed 稳定正 | 写“泛化有效” |
| G_ablation | Mini/SPA 解耦有明确机制解释 | 保留 5 组件写法 |
| G_negative | 若大场景持续负 | 写“anchor reallocation 场景依赖”，只报 playroom |

## 6. 资源与纪律

- 当前 LYH 8 卡被 SHARC 实验占用；完整版矩阵已按用户要求叠加到 SHARC 卡；
- 数据当前使用 `/mnt/003/dataset`（T&T/DB/Mip360）；4-28、3-07、1-100 暂不用于本轮；
- **禁止对 `/mnt` 做小文件操作**，结果写 `/home/T0ng/runs`；
- 不杀 SHARC 进程；不要使用密码；
- 长任务结果在 `/home/T0ng/runs`，最终回写 `docs/data/experiments.csv` 和 `docs/03-reports/`。

## 8. 完整版列入论文后：现在需要的实验清单

> 前提：完整版 MiniSplat（blur split + contribution area + simplification）将作为论文第五个组件呈现。当前 6 个泛化 cell 已出结果，T&T truck 仍在重试；以下实验按“能把这一点的贡献写稳”的优先级排列。

### 8.1 必做实验（决定论文主表与结论）

| ID | 要回答的问题 | 配置 | 场景 | 规模/预计时长 | 资源 | 判定标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | 完整版是否跨场景有效 | full ±，SPA 0.85，30k，`mini_splat_views=8`，其余与 cell2 一致 | playroom、drjohnson、T&T train/truck、Mip garden/bicycle/stump | 7 个 depth-reinit 对照组 + 已有 6 个 full；每个 1–3h | A100×7，可叠加 | ≥4/7 场景 PSNR 增益 >0，且替代大场景不显著为负 | full 已多数完成，缺 truck；对照组需补 |
| A2 | 完整版在预算曲线上的性价比 | full ±，r={0.52,0.85,0.92,0.97}，30k | playroom | 4 个 full + 复用 4 个 depth 点；每个 1–1.5h | 4 GPU | BD-PSNR ≥ +0.05 dB 或 BD-rate ≤ −3% | 未跑 |
| A3 | 大场景是否仍然负 | full ±，SPA 0.85，110k；再加非 SPA full | 替代数据集（待选，不用 4-28/3-07/1-100） | 2–3 个 110k；每个约 4–8h | 1–2 GPU | 同 λ/预算下 PSNR 增量 ≥ +0.05 dB 且体积 <+5% | 暂停，等数据集 |
| A4 | 结果是否依赖 seed | seed=42、2026，full ±，30k | playroom、drjohnson、T&T train | 6 个 full + 6 个 depth；每个 1–2h | 6 GPU | ≥3/5 场景跨 seed 正，std < gain/2 | 部分有旧 seed 数据，未完整 |
| A5 | 完整版的增益来自哪个机制 | full / depth-only / blur-only / simplify-only | playroom，30k | 4 个 cells，每个 1–1.5h | 4 GPU | 拆出 blur split 与 contribution simplification 各自的贡献 | 未跑 |
| A6 | 体积与质量指标是否可信 | 同 A1，附 MLP 量化 ± | 每个场景 | 复用 A1 ckpt + compress/decode；每场景约 0.5h | 1 GPU | 完整版与 depth 版的 PSNR、SSIM、LPIPS、total_MB、编码锚点数都要对齐 | 部分完成，需统一收集 |

### 8.2 建议实验（增强论文严谨性）

| ID | 要回答的问题 | 配置 | 场景 | 规模/预计时长 | 资源 | 判定标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B1 | `views` 是否影响结论 | `mini_splat_views=8/16`，full，30k | playroom、drjohnson | 2×2=4 cells | 4 GPU | 两档结论方向一致 | 未跑 |
| B2 | `max_new` 是否敏感 | max_new=2k/4k/8k，full，30k | playroom | 3 cells | 3 GPU | 选择误差最小、质量最高配置 | 未跑 |
| B3 | 与 MLP 量化的交互 | full + MLP quant ± | playroom、替代大场景 | 4 cells | 2 GPU | 量化不使完整版增益消失 | 部分可复用 |
| B4 | 训练时间/峰值显存 | full vs depth | 每类场景 1 个代表 | 测量脚本 | 1 GPU | 报告增量，不设硬门 | 未测 |
| B5 | 位流组成与效率 | full ckpt | playroom、替代大场景 | 分析 + `codec_roundtrip_diagnostics` | CPU/GPU | bit-exact mismatch=0 | 部分完成 |

### 8.3 可选/后续实验（审稿人可能追问）

| ID | 内容 | 说明 |
| --- | --- | --- |
| C1 | 视觉对比和误差热图 | playroom、drjohnson、替代大场景，full vs depth vs HAC++ |
| C2 | 锚点分布统计 | contribution area、最近邻密度、valid offsets/anchor |
| C3 | 编码/解码延迟 | 与 HAC++ 同口径 |
| C4 | 负面结果表 | I1、P0、R、I5、I6 side-info、语义 prior |
| C5 | 发布与复现 | 配置、随机种子、代码分支、结果 JSON 回写 |

### 8.4 当前首个可交付点

- 已完成：playroom、drjohnson、T&T train、Mip garden/bicycle/stump 的 full 结果；
- 未完成：T&T truck（重试中）、7 个同 protocol depth-reinit 对照、替代大场景 110k（待定数据集）、多 seed、预算曲线；
- 建议先做：A2 + A3 + A1 控制组；这三项完成后即可给论文主表与 BD 曲线。

### 8.5 新大场景运行数（1-78 / 2-06 / 4-10）

**一个大规模数据集最低要跑 2 个，建议跑 3 个训练。**

| 运行 | 配置 | 目的 |
| --- | --- | --- |
| full | MiniSplat 完整版 + SPA 0.85，110k | 新方法的候选操作点 |
| depth | MiniSplat depth-reinit + SPA 0.85，110k | 同协议对照，隔离“完整版”贡献 |
| baseline | 无 MiniSplat + SPA 0.85，110k | 与 SPA 基线比较，证明不会因 mini 而变差 |

如果只回答“完整版 vs depth-reinit”，用前 2 个；如果写进论文主表并做三向对比，用全部 3 个。推荐方案：

```text
3 个数据集 × 3 个训练 = 9 个 110k cells
```

多 seed 和大规模 RD 曲线延后：seed 稳定性每个配置×2 seed、每个数据集可能需 4–6 个额外训练。

### 8.6 新大场景磁盘与执行纪律

1. 每次只解压一个场景到本地，跑完该场景的全部 cells 后删除原始解压数据；
2. 重要输出（bitstream、metrics、ckpt）保留在 `/home/project2/runs`；
3. 原始归档始终留在 `/mnt/newproject2/data/_archives`，不删除；
4. 每场 run 的评估口径：`data_factor=1`、`max_width=1600`、`test_every=8`、解码后 PSNR/SSIM/LPIPS。
