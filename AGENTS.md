# agent.md — DCCA-GS 代理工作须知

> 本文件对在 DCCA-GS 仓库（`/Users/chen/Documents/DCCA-GS`）工作的所有 Agent 生效。
> 首次建立：2026-09-20。目前只有一章核心要求，后续可在文末追加，不得删除已有内容。

## dcca 变更记录维护要求

- 对 dcca 项目的任何变更，包括代码、配置、依赖、文档、接口、数据模型、部署流程、运维流程、工作流（含 `.agents/skills/` 下的 skill）等，必须在任务结束前或提交/PR 前更新变更记录文档。
- 变更记录文档路径：`docs/CHANGELOG_DETAILED.md`（全时序变更记录，2026-08-08 项目起点至今；维护规则与证据优先级见该文档 §0）。
- 每次更新必须包含：日期、版本/提交哈希（未提交时写"未提交"）、变更类型、摘要；尽量补充：详细说明、影响范围、关联文件/PR/Issue、迁移与兼容性说明、证据来源。
- 无证据不得编造；缺失信息标注"待确认"。不删除、不覆盖已有记录；写错的记录追加"更正"条目。
- 提交或合并前检查该文档是否已同步更新。
- 实验数字不写在变更记录里，先登记进 `docs/data/experiments.csv`（数字唯一事实源），再在报告中引用；变更记录只记"发生了什么变更"（见 `docs/README.md` 命名与维护规则）。
- 后续 Agent 在执行任何 dcca 相关任务时，必须遵守本要求。
